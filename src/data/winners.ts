export type StaticWinner = {
  position: string;
  teamName: string;
  prize: string;
  description: string;
  image: string | null;
};

export const WINNERS: StaticWinner[] = [
  // Populate after the hackathon.
];
